<% provide(:title, "Top") %>
<div class="center">
  <h1 class="top-logo">個性が分かる心理テスト<br>JCITA - BASIC</h1>
  <!-- ログイン状態時はログインボタン無効にする -->
  <% if logged_in? %>
    <%= link_to "ログイン状態です", '#', class: "btn btn-lg btn-primary disabled", disabled: true %>
  <% else %>
    <%= link_to "Login", login_path, class: "btn btn-lg btn-info" %>
  <% end %>

  <% if !logged_in? || admin_user? %>
    <p>アカウント作成は<%= link_to "こちら", signup_path %>から</p>
  <% end %>
</div>
