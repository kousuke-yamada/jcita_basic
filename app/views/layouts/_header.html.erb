<header class="navbar navbar-fixed-top navbar-inverse">
  <div class="container">
    <%= link_to "猫でも出来る🐈‍⬛タイピング練習", root_path, id: "logo" %>
    <nav>
      <ul class="nav navbar-nav navbar-right">
        <% if logged_in? %>
          <% if admin_user? %>
            <li><%= link_to "ユーザー一覧", users_path %></li>
          <% end %>

          <li><%= link_to "モード選択", current_user %></li>
          <li><%= link_to "マイスコア", ranking_user_path(current_user) %></li>
          
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              👤 <%= current_user.name %> さん<b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <% unless admin_user? %>
                <!-- <li><%= link_to "モード選択", current_user %></li>
                <li><%= link_to "マイスコア", ranking_user_path(current_user) %></li> -->
                <li><%= link_to "アカウント情報", edit_user_path(current_user) %></li>
              <% end %>
              
              <li class="divider"></li>
              <li>
                <%= link_to "ログアウト", logout_path, method: :delete %>
              </li>
            </ul>
          </li>
        <% else %>
          <li><%= link_to "ログイン", login_path %></li>
        <% end %>
      </ul>
    </nav>
  </div>
</header>